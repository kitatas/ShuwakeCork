namespace Furu.Common
{
    public enum SceneName
    {
        None,
        Boot,
        Main,
    }

    public enum LoadType
    {
        None,
        Direct,
        Fade,
    }

    public enum RankingType
    {
        None,
        Distance,
    }
}